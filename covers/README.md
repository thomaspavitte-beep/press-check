# Covers

Cover images for the completion card and artwork switcher.
Square works best (e.g. 300×300 JPG). Reference a file here from the
`cover:` field in `art/manifest.js`. When a cover is missing the UI falls
back to a lettered tile — nothing breaks.
